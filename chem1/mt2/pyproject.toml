[project]
name = "chem1-mt2"
version = "0.1.0"
description = "CHEMx19A Exam II sheet, built from markdown to PDF"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "markdown>=3.4",
    "weasyprint>=60",
    "Pillow>=10",
    "matplotlib>=3.7",
]

[tool.uv]
package = false
